{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "/bin/sh -c \"exec uvicorn app.main:app --host :: --port ${PORT:-8080} --workers 4\""
  }
}
